{
  "name": "button",
  "version": "0.1.0",
  "dependencies": {
    "@frctl/fractal": "^1.5.13"
  },
  "scripts": {
    "fractal:start": "fractal start --sync",
    "fractal:build": "fractal build",
    "sass:gen": "sass './src/scss:public/css/' --no-source-map",
    "sass:assets": "sass './src/scss:components/button/' --no-source-map",
    "sass:build": "npm-run-all -s sass:gen sass:assets"
  },
  "devDependencies": {
    "@rsm/fractal-pug-adapter": "^0.5.0",
    "npm-run-all": "^4.1.5",
    "pug": "^3.0.2",
    "sass": "^1.52.1"
  }
}
